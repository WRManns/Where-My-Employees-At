import React from "react";
import Sorter from "../Sorter/index.js";

//Uses the hook to Sorter to render the main section of the app
function Main (){
    return (
        <>
        <Sorter />
        </>
    );
};

export default Main;